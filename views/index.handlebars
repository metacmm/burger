<div>
    <img src="/assets/img/burger.jpeg">
    <h3>
    </h3>
    <div>
        <ul>
            {{#each burgers}}
                {{#unless devoured}}
                    <li>{{id}}. {{burger_name}} <button class="change-devour" data-id={{id}}>Devour It!</button></li>
                {{/unless}}
            {{/each}}
        </ul>
        <ul>
            {{#each burgers}}
                {{#if devoured}}
                    <li>{{id}}. {{burger_name}}</li>
                {{/if}}
            {{/each}}
        </ul>
    </div>


    <form class="create-form">
        <input type="text" id="text-burger" name="name">
        <button type="submit" id="btn-submit">Submit</button>
        <p>Enter a burger you want to eat Click the "Submit" button</p>
    </form>
</div>